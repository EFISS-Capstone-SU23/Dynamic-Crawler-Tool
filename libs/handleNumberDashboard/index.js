import driver from '../../utils/chromeDriver.js';

export default function handleNumberDashboard(url) {
	// TODO: Implement this function
	driver.get(url);
	// Return array containing every link on the page
}
